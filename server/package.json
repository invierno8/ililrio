{
  "name": "rio-jynx-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared Jynx comment thread for the RIO demo, persisted to the repo.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
